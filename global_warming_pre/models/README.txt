# Trained model artifacts are saved here by train_and_save.py
# or by clicking "Train All Models" inside the dashboard.
#
# Files written here:
#   best_model.pkl     — best fitted sklearn-compatible model
#   scaler.pkl         — StandardScaler
#   label_encoder.pkl  — LabelEncoder for Country column
#   feature_names.pkl  — ordered list of feature names
